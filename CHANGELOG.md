# 변경 이력

MLsoft 사내 연차·복리후생 관리 시스템 V2.

버전은 **아직 배포된 적이 없다** — `0.x`는 "운영 투입 전"이라는 뜻이고,
`1.0.0`은 운영 DB에 실사원 데이터가 들어가는 시점에 붙인다.
그때까지 스키마는 backfill로 이어 붙인다 (docs/12 머리말의 실행 순서 참고).

기준: 날짜는 KST. 각 항목 끝의 괄호는 참조처(`docs/`의 항목 번호 또는 QA 점검표 번호).

---

## [Unreleased]

> 현재 구현 상태와 우선순위의 단일 원본은 [`docs/12-남은-작업.md`](docs/12-남은-작업.md)다.
> 이 절은 변경 이력과 다음에 착수할 큰 범위만 기록하며, 상태표를 복제하지 않는다.

### 남은 코드 작업 정리 (2026-09-07)

- **묶인 발송 복구 기준 정정** — `recoverStaleSending`이 `created_at`으로 판정해, 재시도로 다시 집힌
  오래된 행이 선점 직후 복구 대상이 되어 발송 중인 메일이 한 번 더 나갈 수 있었다.
  `email_history.sending_at`(선점 시각)을 추가하고 그 값으로 회수한다. backfill 14번.
- **공휴일 조회 결과 구분** — 키 없음·호출 실패·이상 응답·정상을 `Outcome` 4종으로 나눴다.
  자동 조회는 지금처럼 조용히 degrade하고 캐시를 지키며, 관리자 수동 동기화·키 검증만
  원인별 오류(`HOLIDAY_API_*` 3종)로 표면화한다. 검증 응답에서 항상 참이던 `valid`를 걷어냈다.
- **관리자 컨트롤러 테스트** — 양식 6건·연동 7건을 추가하고, `SecurityAccessMatrixTest`에는
  실제 JWT 쿠키·필터 체인을 통과하는 세 관리자 API의 역할 게이트를 넣었다.
- 검증: 백엔드 439 · 프론트 254(39파일) · lint 0 · build 정상.

### 퇴직자 데이터 파기 · 재입사자 처리 (2026-09-08)

설계 두 건을 순서대로 구현했다.

**퇴직자 데이터 파기** (`docs/설계-초안/퇴직자-데이터-파기-설계-2026-09-07.md`)

- 파기 방식을 정책으로 열었다 — `retiree_purge_mode`(MANUAL/AUTO)와 `retiree_purge_years`(3~10).
  **기본이 MANUAL이라 관리자가 켜기 전에는 아무것도 사라지지 않는다.** 하한 3년은 근로기준법
  보존기간이라 낮출 수 없다.
- 파기는 행을 지우지 않고 **누구인지를 지운다** — 이름·이메일·생일·입사일과 연차/복리후생 사유,
  발송된 메일 본문까지. 연차 사용 통계와 결재 이력은 남는다. `admin_audit_log`는 감사 근거라
  예외로 보존하되 파기 기록에 이름을 남기지 않는다.
- 자동 모드는 스케줄러 ⑤로 **30일 예고 후** 실행한다. 사람이 누르지 않는데 되돌릴 수 없기 때문이다.
  예고를 받고 보류를 걸면 대상에서 빠진다. 수동 모드는 메일을 보내지 않고 화면 배지로만 알린다.
- 관리자 화면은 퇴직 탭을 확장했고, 파기 실행에는 **사원 이름 직접 입력**을 요구한다.
- 정책 카탈로그에 조건부 노출(`visibleWhen`)을 더했다 — 연수 설정은 AUTO일 때만 보이는데,
  프론트에 키 이름을 하드코딩하지 않으려면 서버가 의존 관계를 내려줘야 한다.

**재입사자 처리** (`docs/설계-초안/재입사자-처리-설계-2026-09-07.md`)

- 퇴직으로 근로관계가 끊기면 계속근로연수가 0부터 시작한다. 지금까지는 그 경로가 없어 퇴직
  복구를 쓸 수밖에 없었는데, 복구는 근속이 이어진 것으로 잡혀 **연차가 과다 부여되고 공백
  기간이 catch-up으로 정산**됐다.
- `last_reset_date`를 재입사일로 세우는 것이 핵심이다 — 이 한 줄이 공백 catch-up을 막는다.
- 이전 근속의 살아 있는 신청은 같은 트랜잭션에서 종결시킨다. 잔액을 0으로 만드는데 `PENDING`이
  남으면 `use_days` 불변식이 깨진다.
- 역할·직책은 승계하지 않는다. 팀장·총관리자가 재입사만으로 권한을 되찾으면 안 된다.
- 지나간 근속은 `employment_periods`에, 현재 근속은 `users`에 둔다. 같은 사실을 두 곳에
  저장하지 않는다. 내 연차 내역은 구간으로 나눠 보여주고, 이전 근속이 없으면 구간 UI가 아예 없다.

검증: 백엔드 488 · 프론트 301(41파일) · lint 0 · build 정상. 운영 대상 backfill 17개.

### 캘린더 키보드 이동·자기 퇴직 회귀 (2026-09-07)

- **캘린더를 키보드로 넘길 수 있다.** `←`/`→`·`PageUp`/`PageDown`으로 월 이동, `Home`으로 이번 달.
  입력 중이거나 모달이 열려 있거나 조합키가 눌린 상태에서는 가로채지 않는다. 전역 리스너가 아니라
  캘린더 컨테이너에 붙이고, 모바일 스와이프와 같은 상태 변경 함수를 재사용한다.
- **자기 퇴직 가드에 회귀 테스트를 붙였다.** 가드는 2026-08-17에 들어갔지만 테스트가 없었다 —
  유일한 관리자가 본인을 퇴직시킬 때 `LAST_SYSTEM_ADMIN`보다 `CANNOT_RETIRE_SELF`가 먼저 걸리는지,
  화면에서 본인 행 버튼이 비활성이고 사유가 보이는지를 고정했다.
- 검증: 백엔드 456 · 프론트 277(41파일) · lint 0 · build 정상.

### 미래 차감 연차 분리 (2026-09-07)

- **다음 기산일 이후 날짜의 연차가 현재 회차 잔액을 먹지 않는다.** 내년 3월 연차를 예약하면
  올해 잔액이 줄었고, 잔여 0이면 아직 부여되지도 않은 다음 회차 연차를 예약조차 할 수 없었다.
- 회차 창 `[기산일, +1년)`을 **신청·복구·리셋 이월** 세 지점이 공유한다. 불변식이 한 줄이 됐다 —
  `use_days` = 살아 있는 신청 중 현재 회차 창 안의 날짜 합.
- 리셋 채무·이월 보너스·소멸분에서 `carriedUse` 뺄셈을 걷어냈다. 새 모델에서는 `use_days`에
  미래분이 없어 두 번 빼는 셈이 된다. 경계 두 가지로 기존 결과와 같음을 검산했다(설계 §3).
- 다음 회차 예약 한도는 `정책 연차 − 현재 빚`이고 당겨쓰기를 적용하지 않는다. 예약은 다음
  1회차까지만 받는다(`next_cycle_reservation_enabled`, 에러 코드 2종 추가).
- 잔여를 읽는 화면 4곳에 "다음 회차 예약" 줄을 붙이고, 신청 패널은 선택 날짜를 회차별로 나눠
  계산한다. 기존 데이터는 backfill 15번이 재계산한다.
- 검증: 백엔드 455 · 프론트 270(41파일) · lint 0 · build 정상.

### 문서 정합성 점검 (2026-09-06)

- 문서 전수 감사에서 나온 수치·상태 모순을 일괄 정정했다 — 스케줄러 잡 개수(3→4), `schema.sql`
  테이블 수(16·17→20), 엔티티 수(19→18, `BaseTimeEntity`를 잘못 세고 있었다), 엔드포인트·화면·테스트
  수치, 운영 대상 backfill 12→13, 정책 카탈로그 `PENDING_FEATURE` 2→0.
- `docs/03`에 연동 저장 경로 2개를 구체 경로로 나누고, 처리 이력 `my-approvals` 권한을 실제 게이트(TL·SA)에
  맞췄다. `docs/01`의 온보딩 판별 기준을 `onboarding_status`(리뷰 S-1)로 고쳤다.
- 동결 리포트(07·08)는 본문을 두고 상단 상태표만 갱신했다. 인덱스에서 빠져 있던 QA 문서 1건을
  `docs/설계-초안/README.md`에 연결했다.

### 공휴일 마무리 (2026-09-05)

- 공휴일 외부 조회와 캐시 저장 트랜잭션을 분리하고 동기화 응답을 `{ year, count }`로 통일했다.
- 관리자 연차 정책 화면에 공휴일 연도 선택·적재 건수·수동 동기화 UI와 회귀 테스트를 추가했다.
- staging credential 시드 절차와 공휴일 화면·degrade·자동 적재 QA 항목을 문서화했다.

### 이메일 2단계 W2 (2026-09-05)

- 공통 AES-GCM `SecretCipher`와 `APP_CREDENTIAL_ENCRYPTION_KEY`·기존 키 fallback을 적용했다.
- 메일 계정 DB 암호화 저장·회전·마스킹 조회와 발송 sender 캐시·환경변수 fallback을 연결했다.
- 이메일 이력의 `PENDING/FAILED → SENDING` 원자 선점과 중단 잔재 복구를 추가했다.
- 온보딩 대기·승인·반려·1회 수정 알림을 `NOTICE` 아웃박스에 연결했다.

### 이메일 2단계 W3 (2026-09-05)

- 연차 소진 안내의 D30·D60·D90·QUARTER 대상 판정, 중복 방지 이력, 아웃박스 발송과 ④번째 스케줄러 잡을 연결했다.
- 리마인더 양식 DB 조회·기본 문구·변수 치환과 HTML escaping, `reminder_auto_cycle` 활성화·설정 교차검증을 추가했다.

### 이메일 2단계 W4a (2026-09-05)

- 관리자 이메일 대상·일괄 발송·이력·FAILED 재발송 API와 일 회 400건/회 100명 상한, 수신자 마스킹을 추가했다.
- 이메일 양식 조회·수정·미리보기와 리마인더 발송과 동일한 변수 치환·escaping 경계를 연결했다.
- SMTP·공휴일 자격 증명 마스킹 조회·암호화 저장·회전·테스트/검증 API와 관리자 감사 기록을 추가했다.
- 이메일 설정 변경·정리와 관련 문서를 갱신했으며 `/admin/integrations`·`/admin/emails` 화면은 별도 워커 범위로 남겼다.

### 완료 (2026-08-20 밤)

- **연차 사용 히트맵**(잔디 그래프) — 내 정보(사용 일수)·팀 관리(쉬는 인원 수).
  연 단위 전용 API. **팀 응답은 날짜와 인원 수만** 담아 개인정보가 샐 경로를 없앴다 (`FEATURE-1`, 구 D-1)
- **직책 자가 설정** — 내 정보에서 50자 자유 입력 (`FEATURE-3`, 구 D-3)
- 사유 말줄임 + 클릭 시 모달 상세 (`FEATURE-4`, 구 D-4) · 퇴직 경과일수 (`FEATURE-2`, 구 D-2) ·
  "팀 캘린더"→"캘린더" (`FEATURE-5`, 구 D-5) · 캘린더 월 이동 화살표 (`FEATURE-6`, 구 D-6)
- 미배정 부서 식별 플래그·기본 부서 잠금·팀장 승격 시 부서 선택 흐름 (`USER-9`, 구 B-9; 2026-08-22)
- 팀 정보에서 `부서 미배정`과 `미지정`을 구분하는 표시와 회귀 테스트 (`BUG-1`, 구 E-1; 2026-08-22)
- 모바일 1단계 — 기준선·서랍·`ResponsiveTable`·참조 화면

### 모바일 캘린더 2단계 (2026-08-24~25)

- 안드로이드 캘린더형 정사각 날짜칸·인접 월 날짜·선택일 하단 일정 요약과 `M월 D일에 추가 +`를 반영했다.
- 일정 종류별 색 점, 좌우 스와이프 월 이동, 모바일 등록 하단 시트를 반영했다.
- 모바일 셀의 `...더보기`는 제거하고 데스크톱에서만 일정 상세 모달 진입 손잡이를 제공한다.
- 다음 작업은 공휴일 흐름 보완 → 이메일 계정/키 보안과 일괄 발송 → 자동 검증 → 사용자 QA 순서다.

### 공휴일 API·일정 통합 1단계 (2026-08-25)

- 테스트 키로 data.go.kr 2026년 응답(22건)을 확인했다. 키 원문은 소스·문서·로그·커밋에 남기지 않는다.
- `holiday_api_credentials`를 추가해 제공자별 API 키를 AES-GCM 암호문으로 한 번만 저장하고,
  `holidays`에는 날짜·명칭만 저장하도록 분리했다. 암호화 키는 `HOLIDAY_CREDENTIAL_ENCRYPTION_KEY`로
  별도 주입한다. 최초 시드는 `HOLIDAY_CREDENTIAL_SEED=true`인 경우에만 수행해 이후 DB 키를
  환경변수가 덮어쓰지 않게 했다.
- 캘린더에서 공휴일을 `HOLIDAY` 일정 이벤트로 통합했다. 데스크톱 상세 모달·초과 일정 계산·모바일
  색 점·선택일 하단 요약에 같은 이벤트 모델을 사용한다.
- API `resultCode` 정상 응답 검증과 공휴일 파서 회귀 테스트를 추가하고, 외부 호출이 긴 DB 트랜잭션을
  점유하지 않도록 조회 경계를 분리했다. 백엔드 368건·프론트 227건 자동 검증을 통과했다.

### 다음 작업 후보 (상세 상태는 docs/12)

- 🔴 **USER-1** 미래 차감 연차 분리 설계(구 B-1)
- 🔴 이메일 계정 관리자 설정·일괄 발송·수동 재발송·관리자 문의(구 B-4·B-5·B-8)
- 🟡 **MOBILE-1~4** 모바일 바텀시트·정책/부서 관리·캘린더 다중 날짜 선택 흐름(구 C-1~C-4)
- 🟡 캘린더 키보드 월 이동 및 일정 상세 진입 개선
- 🟡 **SEC-8·SEC-9** 팀장 부서 이동 정책·동시성 처리(구 S-8·S-9)


### 모바일 대응 (진행 중)

- **1단계 완료 (2026-08-20)** — 기준선·앱 껍데기·표 전환 방법 + 참조 화면 1개.
  나머지 14개 화면은 다음 회차다 (docs/12 B-10)
  - 브레이크포인트: 앱 셸 `lg`(1024px) · 표 전환 `md`(768px).
    사이드바 236px + 패딩 60px를 빼면 768px에서 **472px만 남아** 560px 표조차 안 들어간다
  - 좁은 화면 사이드바 → **왼쪽 서랍**. 역할별 메뉴가 6·8·12개라 하단 탭(4~5개 한계)에 안 맞는다
  - `ResponsiveTable` — 표와 카드를 **CSS로 숨기지 않고 렌더 트리를 나눈다.**
    숨긴 복제본이 접근성 트리에 남아 같은 행을 두 번 읽는 것을 막는다
  - 모달은 640px 미만에서 전체 화면 시트 (키보드가 올라올 때 중앙 모달은 이중 스크롤이 생긴다)
  - 참조 구현: `AdminHistoryPage` — 필터·탭·배지·페이지네이션과 6~8열 표를 다 갖고 있다
- **데스크톱 렌더는 그대로다.** 기존 테스트 187건이 하나도 깨지지 않은 것으로 확인했다

## [0.9.0] — 2026-08-20

디자인 개편 브랜치(`design/cobalt-glass-redesign`)에서 **1차 수동 점검을 완주**하고
그 결과를 반영한 판. 기능 추가보다 **"만든 것이 실제로 보이고 눌리는가"**를 메운 회차다.

### 새로 생긴 것

- **캘린더 하루 상세 모달** — 한 날짜에 항목이 몰리면 `+N개` 손잡이로 그날 전체를 편다.
  날짜 셀 클릭은 기존 등록 패널을 그대로 연다 (B-2)
- **대시보드 다가오는 내 일정** — 본인 일정 + 공휴일을 가까운 순 5건. 메모는
  **호버·포커스일 때만 DOM에 그린다** (opacity로 감추면 접근성 트리에 남는다) (B-3)
- **저장 안 한 변경 붙잡기** — 연차 정책·내 정보. 미저장 배지 + 이탈 확인 창 (S절)
- **팀장 승격 시 부서 함께 지정** — 전용 엔드포인트 `PATCH /api/users/{id}/role-and-department`.
  한 트랜잭션이라 역할 변경이 실패하면 부서 배정도 롤백된다 (B-7, QA 8-4d)
- **총관리자 자기 결재** — 총관리자는 본인 신청의 승인자가 된다 (QA I-19)

### 바뀐 것

- **라우터를 데이터 라우터로 교체** (`BrowserRouter` → `createBrowserRouter`).
  `useBlocker`를 쓰게 되면서 **브라우저 뒤로가기까지** 이탈 가드가 덮고,
  링크 판별 코드(수정키·target·origin·현재 경로)가 전부 사라졌다
- **결재 자기 승인 금지에 총관리자 예외** — 2026-08-17 규칙을 뒤집었다.
  총관리자 위에는 결재선이 없어, 막아 두니 **총관리자가 한 명일 때 신청 자체가 실패**했다
- 승인자 결정 규칙에 ⓪단계 추가 — 신청자가 SYSTEM_ADMIN이면 본인 (docs/01 §2-3(a))

### 고친 것

- **`PageImpl` 직렬화** — 백엔드가 `PagedModel`이 아닌 형태를 내보내 **모든 페이지 버튼과
  배지가 조용히 죽어 있었다.** 양쪽 테스트가 각자 옳다고 믿는 형태를 가정해 아무도 못 잡았다
- **모달 클릭아웃** — 안에서 누르고 밖에서 떼면 닫히던 것. 입력 중 드래그로 내용이 날아갔다
- **내 정보 폼이 빈 칸으로 굳는 문제** — `useState` 초기값은 첫 렌더에만 쓰이는데
  localStorage 부트스트랩이 낡거나 없으면 그대로 굳었다
- 목록 페이지네이션 7곳 · 부서 계층 정렬 · 메일 바로가기 수신자 분기 · 팀장 공석 처리

### 점검표 정정 (코드가 아니라 절차가 낡았던 것)

- **I-9** 절차에 "설정 저장을 눌러 미저장 배지가 사라지는 것까지 확인" 추가 —
  이 단계가 없어 1차 점검에서 실패로 잡혔다 (재점검 통과 확인)
- **J-1** 본인 퇴직 버튼이 비활성이라 "눌러서 실패를 본다"가 수행 불가 →
  API 직접 호출로 서버 가드를 보게 변경
- **F-7 / F-7b** 퇴직자 차단은 401이 맞지만 `staleTime` 30초라
  **직후 메뉴 클릭은 요청이 안 나간다.** 결함이 아니라 SPA의 정상 동작
- **I-12** 기대값 전환 (막기 → 그 자리에서 부서 선택)

### 알려진 문제

- 🔴 **"미배정"이 실제 부서 행이다** — `DataInitializer`가 그 이름의 부서를 만들고
  신규 가입자를 배속한다. 그래서 `department == null`인 계정이 사실상 없고,
  **이번에 만든 팀장 승격 부서 선택이 실계정에서는 도달하지 않는다.**
  **방향 결정됨 (2026-08-20)** — 부서는 그대로 두고 "미배정 소속"도 "부서 없음"과
  같이 취급해 그 확인 창을 띄운다. 착수 전 (docs/12 B-9)
- **미래 차감 연차** — 기산일 이후 날짜도 현재 잔액에서 빠진다. 리셋 쪽 이월은 되어 있고
  신청 시점에 기산일 개념이 없다 (docs/12 B-1)
- **온보딩 승인 배지** — 1차 점검에서 실패로 보고됐으나 코드에서 재현되지 않았다.
  추측 수정을 하지 않고 남겨 뒀다 (QA 1-6)

### 검증

| | 1차 점검 전 | 현재 |
|---|---|---|
| 백엔드 테스트 | 339 | **350** |
| 프론트 테스트 | 157 | **187** |
| 수동 점검 | — | 95항목 중 58 통과 · 5 실패(→ 4 해소) · 32 미확인 |

**뮤테이션 테스트를 이 회차의 검증 기준으로 썼다.** 통과하는 테스트를 믿지 않고
코드를 일부러 망가뜨려 검증이 실패하는지 확인했다 — 그 과정에서
**헛통과하던 검증 5건**을 찾아 고쳤다(빈 검증 3건 포함).

---

## [0.8.0] — 2026-08-16 이전

디자인 전면 개편(코발트 글래스), 개인 일정 도메인, 스케줄러 3잡, 이메일 알림,
관리자 감사 로그, 온보딩 승인 절차, 복리후생 정책 관리.

이 구간은 커밋 메시지와 `docs/11`(전체 흐름 지도)·`docs/07`·`docs/08`·`docs/10`(리포트)에
남아 있다. 소급해 항목을 나누지 않았다 — **문서가 이미 그 역할을 하고 있고**,
이력을 두 곳에 두면 한쪽이 낡는다.
