# 04. 코드 스타일 가이드

> 이전 프로젝트(참고자료/MLsoft) 코드에서 추출한 본인 스타일.
> 새 프로젝트에서도 이 스타일을 유지하되, "고칠 것" 항목은 이번에 바로잡는다.
> 최종 갱신 2026-08-07 — Tailwind v4 정정, `@AuthenticationPrincipal`, 파생 필드 규칙,
> 설정 카탈로그, React Query 규칙(F-1·F-5·F-6)을 실제 코드 기준으로 반영했다.

## 백엔드 (Java / Spring Boot)

### 엔티티
```java
@Entity
@NoArgsConstructor(access = AccessLevel.PROTECTED)  // 무분별한 기본 생성자 방지
@Getter                                             // Setter 금지
@AllArgsConstructor
@Builder
@Table(name = "users")
public class User extends BaseTimeEntity { ... }
```
- **Setter 없이 도메인 메서드로만 상태 변경**: `approve()`, `cancel()`, `resetAndApplyPolicy(...)` 처럼 상태 전환·검증을 엔티티 안에 둔다.
- **정적 팩토리 메서드** 선호: `User.create(...)`, `LeaveRequest.create(request, user)`
- `BaseTimeEntity` 상속으로 `createdAt` 자동 관리 (`@CreatedDate` + `@EnableJpaAuditing`)
- Enum은 `@Enumerated(EnumType.STRING)` — 절대 숫자 저장 금지
- **연차 등 일수 수치는 double/Double 금지 → `BigDecimal`** (DB는 DECIMAL(4,1)). 비교는 `==`가 아니라 `compareTo` (부동소수점 오차 방지, 검증 R-1)
- User 엔티티에 `@Version` 낙관적 락 (동시 신청 초과 방지, 검증 R-5)
- **파생 필드는 계산하지 말고 재계산 한 곳으로 모은다.** `advance_days`가 그 예다 —
  도메인 메서드마다 이 필드를 더하고 빼던 것이 결함 4건(I-1·I-2·I-4·I-8)의 공통 원인이었다.
  지금은 `User.syncAdvanceDays()` 하나만 이 필드에 쓰고, 잔액 3필드(base/bonus/use)를 바꾸는
  도메인 메서드 6개가 마지막에 이걸 호출한다 — `resetAnnualLeave`까지 예외 없이.
  **이 필드에 단독 대입하는 코드를 새로 만들지 말 것** (docs/11 §3-2에 배경)

### DTO
- **Java record** 사용 (불변):
```java
public record AddLeaveRequest(
        String leaveType,
        List<LocalDate> dates,
        String reason,
        Long subApproverId
) {}
```

### 컨트롤러
- 모든 응답 `ResponseEntity<CommonResponse<?>>` 래핑
- **메시지 하드코딩 금지 — 상수(변수)로 관리**. 성공 메시지는 `ResponseMessage` 상수 클래스에 모은다:
```java
// global/response/ResponseMessage.java — 성공 메시지 전용
public final class ResponseMessage {
    public static final String LEAVE_CREATED  = "연차 신청이 완료되었습니다.";
    public static final String LEAVE_APPROVED = "승인 처리가 완료되었습니다.";
    public static final String LEAVE_CANCELLED = "연차 신청이 취소되었습니다.";

    private ResponseMessage() {} // 인스턴스화 방지
}

// 사용 — 컨트롤러에서 문자열 리터럴 직접 쓰지 않기
return ResponseEntity.ok(CommonResponse.success(ResponseMessage.LEAVE_CREATED, data));
```
- 실패는 예외 던지기 (컨트롤러에서 try-catch 금지)
- `@PreAuthorize("hasRole('TEAM_LEADER') or hasRole('SYSTEM_ADMIN')")` 메서드 레벨.
  **`@PreAuthorize`는 역할 게이트 전용**이고, 소유권·승인자 식별 검증은 서비스 계층 책임이다
- 본인 식별은 **`@AuthenticationPrincipal AuthUser`에서** (요청 body의 userId/employeeId 신뢰 금지)

### 예외
- `RuntimeException` 직접 사용 금지 → 커스텀 예외 (`UserNotFoundException` 등)
- **에러 메시지도 변수로 관리** — `ErrorCode` enum에 HTTP 상태 + 메시지를 함께 정의 (docs/03의 에러 코드 표와 1:1 대응):
```java
// global/exception/ErrorCode.java
@Getter
@RequiredArgsConstructor
public enum ErrorCode {
    USER_NOT_FOUND(404, "사용자를 찾을 수 없습니다."),
    ACCESS_DENIED(403, "접근 권한이 없습니다."),
    INSUFFICIENT_LEAVE_BALANCE(400, "잔여 연차가 부족합니다."),
    OVERLAPPING_LEAVE_REQUEST(409, "이미 신청된 기간과 중복됩니다."),
    ALREADY_PROCESSED(400, "이미 처리된 신청입니다.");

    private final int status;
    private final String message;
}

// 커스텀 예외는 ErrorCode를 물고 다닌다
public class BusinessException extends RuntimeException {
    private final ErrorCode errorCode;
    public BusinessException(ErrorCode errorCode) {
        super(errorCode.getMessage());
        this.errorCode = errorCode;
    }
}
```
- `GlobalExceptionHandler`(@RestControllerAdvice)에서 `ErrorCode`의 status/message로 일괄 변환 → 예외 타입마다 핸들러 만들 필요 없이 `BusinessException` 하나로 처리 가능
- 상태 코드: 404 NotFound / 409 Conflict / 400 BadRequest / 403 Forbidden

### 서비스
```java
@Slf4j
@Service
@RequiredArgsConstructor   // 생성자 주입
public class LeaveService {
    @Transactional          // 쓰기 메서드에만
    public void addLeave(...) { ... }

    private User findUserOrThrow(String email) { ... }  // 검증은 private 헬퍼로 분리
}
```
- 스케줄러: `@Scheduled(cron = "0 0 0 * * *")` + `@Transactional` 조합, `log.info()` 로깅

### 관리자 조정 가능한 정책값 (설정)
- `leave_policy_config` 테이블은 **값만** 들고 있고 카탈로그는 `PolicyConfigKey` enum이 정의한다 —
  키·타입·기본값·허용 범위·라벨·설명·동작여부. **설정을 추가할 땐 이 enum에 상수 한 줄만** 넣으면
  시딩(`DataInitializer`)·저장 검증·관리자 화면 렌더가 모두 따라온다
- **프론트에 키 이름을 하드코딩하지 말 것** — `GET /api/admin/configs`가 메타데이터를 함께 내려준다
- 값 검증은 **저장 시점**에 한다(`PolicyConfigKey.validate`). 읽는 시점에 터지면 잘못 넣은 관리자가 아니라
  **사원의 연차 신청이 실패**한다. 단, 읽을 때도 같은 검증을 다시 통과시키고 어긋나면 기본값 + WARN —
  저장 검증이 없던 시절의 값이 상한을 무력화하는 것을 막는다
- 그 값을 읽는 기능이 아직 없으면 반드시 `PENDING_FEATURE`로 둔다 (관리자 화면에 "미동작" 배지).
  설정처럼 보이는데 아무 일도 일어나지 않는 것이 기존 소진 안내 2키의 문제였다 (리뷰 I-3)

### Repository
- 파생 쿼리 메서드 우선 (`findByStatusOrderByIdAsc`), 복잡한 건 JPQL
- 중첩 필드는 `_` 표기: `findByUser_EmailOrderByIdDesc`
- 목록 조회의 LAZY 연관은 `@EntityGraph`로 함께 적재해 N+1을 막는다.
  단 **컬렉션은 `@EntityGraph`에 넣지 말 것** — 페이징이 메모리에서 처리된다.
  컬렉션은 `@BatchSize`로 배치 조회한다 (리뷰 D-2)

### 주석
- 공개 메서드는 JavaDoc(`/** */`), 내부 로직은 한글 `//` 주석 + 불릿 스타일
```java
/**
 * 연차 승인 또는 반려.
 * - 권한 검증: SYSTEM_ADMIN 또는 지정 승인자만 처리 가능
 * - 승인 시: status APPROVED + 사용일수 차감
 */
```

## 프론트엔드 (React)

### 컴포넌트
- `export default function ComponentName({ props })` — **화살표 함수 컴포넌트 금지**
- Props 구조분해, 상태는 `[value, setValue]` 페어 네이밍
- useEffect 안에서 async 함수 정의 후 즉시 호출 패턴:
```jsx
useEffect(() => {
  const load = async () => { ... };
  load();
}, []);
```

### API 모듈 (src/api/*.js)
- axios 인스턴스 1개 (`withCredentials: true`) + 응답 인터셉터에서 에러 toast 일괄 처리
- 함수 하나 = 엔드포인트 하나, 함수 위에 한글 주석
- 쿼리스트링은 `URLSearchParams`, 기본값은 시그니처에 (`page = 0, size = 8`)

### 상수·유틸
- 상수는 컴포넌트 파일 최상단 `UPPER_SNAKE_CASE` 객체/배열 (`LEAVE_LABEL`, `DAY_LABEL`)
- 날짜는 `YYYY-MM-DD` 통일, 방어적 변환 유틸 (`toDateStr`)

### 스타일 (변경: BEM CSS → Tailwind v4 CSS-first)
- 이전: 페이지별 CSS 파일 + BEM(`stat-card__icon--blue`) → **이번엔 Tailwind CSS**
- **`tailwind.config.js`는 없다.** v4 CSS-first 설정이라 디자인 토큰은
  `frontend/src/index.css`의 `@theme` 블록(`navy-*`·`accent-*`·`ink-*`·`radius-*`),
  커스텀 유틸리티는 `@utility`에 있다. 값의 단일 출처는 `index.css`이고 docs/05가 그걸 문서화한다
  (2026-08-07 정정 — 이 문서의 옛 "config에 등록" 서술은 v3 기준이었다)
- 반복되는 조합은 컴포넌트로 추출 (className 문자열 상수 남발 금지)

### React Query (hooks/use*.js)
- 쿼리 키는 `['leaves', 서브리소스, ...파라미터]` — 접두사로 일괄 invalidate 할 수 있게
- **페이징 파라미터를 쿼리 키에서 빠뜨리지 말 것.** `page`만 넣고 `size`를 빼면 같은 리소스를
  다른 `size`로 읽는 두 화면이 **같은 캐시를 공유**한다. 실제로 사이드바 배지(`size:1`)가
  결재 목록(`size:50`)을 1건으로 덮어썼다 (리뷰 F-1)
- 데이터를 바꾸는 뮤테이션은 **파생 화면까지** invalidate 한다 — 복리후생 승인은 연차 잔액을
  바꾸므로 `['leaves']`도 함께 무효화해야 한다 (리뷰 F-5)
- 목록 화면의 상태 우선순위는 **`loading → error → empty`**. 실패하면 데이터가 빈 배열이라
  empty 조건도 참이 되는데, "데이터가 없습니다"를 보여주면 결재 대기 건이 있는데도 놓친다.
  실패는 `ErrorState`로 구분해 재시도 수단을 준다 (리뷰 F-6)

## 이번에 바로잡을 것 (이전 프로젝트의 실수)

| 이전 | 문제 | 이번 규칙 |
|---|---|---|
| `addBrithDayLeave` / `/add-brith-day-leave` | Birth 오타가 API까지 전파 | 이름 지을 때 맞춤법 확인, 오타는 즉시 수정 |
| `getPaddingLeave` | Pending 오타 + URL에 camelCase 메서드명 노출 | URL은 kebab-case 리소스명만 |
| `DELETE /api/leave/del` | 모호한 URL | `DELETE /api/leaves/{id}` 형태 |
| `canceLeave()` (프론트) | 오타 | 〃 |
| `advance_days` 자바 필드 | 필드명이 snake_case | 자바 필드는 camelCase, 매핑은 `@Column(name=...)` |
| 요청 body에 employeeId 포함 | 위조 가능 | 인증 주체(Authentication)에서 추출 |
| 시크릿 평문 커밋 (yml, 기획서) | 보안 사고 위험 | 환경변수/`.env` + `application-example.yml`만 커밋 |
| 권한 가드: useEffect 내 me() 후 리다이렉트 | 깜박임, 우회 여지 | 라우트 가드 컴포넌트로 선차단 |
