# 프론트엔드 구현 현황

> React 19 · Vite · Tailwind CSS v4 (다크 네이비 테마) · react-router 7 · axios · react-hot-toast · dayjs · lucide-react
> 코드 기준일: 2026-07-05

## ✅ 인증 / 앱 셸 (실 API 연동 — 실동작)
- `App.jsx` — 라우팅 + **역할 기반 라우트 가드**(RequireAuth roles)
- `LoginPage`, `OAuthCallbackPage`, `OnboardingPage` — `/api/auth/*` 실 연동
- `components/layout/Layout`·`Sidebar` — 236px 사이드바 + 콘텐츠 셸, 역할별 메뉴 노출
- `components/RequireAuth` — 미로그인/온보딩 미완료/권한 미달 선차단 (localStorage `userInfo` 기반)
- `api/index.js` — axios 인스턴스(쿠키 전송, 401 시 로그인 리다이렉트), `api/auth.js`
- `constants/roles.js` — `ROLE`, `ROLE_LABEL`

## 🟡 9개 페이지 — 목업 UI (화면 완성, 데이터는 mock)

> ⚠️ 전부 `src/mocks/data.js`의 임시 데이터로 렌더링. **실 API 미연동.** 버튼/신청/저장 등 액션은 `toast()` 안내(mock).

| 페이지 파일 | 라우트 | 접근 권한 | 핵심 구성 |
|---|---|---|---|
| `DashboardPage` | `/dashboard` | 전체 | 잔여 연차 히어로 + 소멸/결재대기 스트립 + 팀 연차 캘린더 |
| `CalendarPage` | `/calendar` | 전체 | 전면 월간 캘린더(월 이동), 내연차/동료/공휴일 색 구분 |
| `HistoryPage` | `/history` | 전체 | 연도·종류·상태 필터 + 통계 스트립 + 신청 내역 테이블 |
| `WelfarePage` | `/welfare` | 전체 | 6개 카테고리 카드 그리드 + 내 신청 내역 |
| `TeamPage` | `/team` | 전체 | 부서 요약 + 팀원 테이블(팀장 강조) |
| `MyInfoPage` | `/myinfo` | 전체 | 프로필 카드 + 연차 요약 + 정보 수정 폼 |
| `ApprovalsPage` | `/approvals` | 팀장·총관리자 | 대기/승인/반려 탭 + 결재 카드(승인·반려 버튼) |
| `AdminMembersPage` | `/admin` | 총관리자 | 재직/퇴직 탭 + 검색·역할 필터 + 구성원 테이블 |
| `AdminPolicyPage` | `/admin/policy` | 총관리자 | 근속년수별 정책 + 시스템 설정 + 소멸 이력 |

## ✅ 공유 기반 (이번 세션 신설)
- `src/mocks/data.js` — 가상 회사 MLsoft(부서 3개·구성원 12명+퇴직 1·연차/복리 신청·공휴일·정책·설정·리셋 이력)의 **정합적 목업 데이터**. `getCurrentUser()`는 실제 로그인 정보(localStorage) + mock 상세를 병합.
- `src/components/ui/` — `PageHeader`, `StatusBadge`(상태 틴트 pill), `Card`(서피스 래퍼)

## ❌ 미구현 / 주의
- **실 API 연동** — 9개 페이지 전부. 백엔드 업무 API 완성 후 전환 필요.
- **연차 신청 폼**(디자인 ③) — 라우트·페이지 없음. 현재 대시보드/캘린더의 "연차 신청" 버튼은 `toast` 안내만.
- **공휴일** — `mocks/data.js`의 하드코딩 목록. 실제로는 `/api/holidays`(data.go.kr) 연동 대상.

## 🔄 실 API 전환 방법 (백엔드 API 완성 후)
1. 각 페이지 상단 `import { ... } from '../mocks/data.js'` → `src/api/*` 호출로 교체
2. 데이터 로딩은 `useEffect` + 로딩/에러 상태 처리 (axios 인스턴스 `api` 재사용)
3. mock 액션(`toast`)을 실제 POST/PATCH 호출로 교체
4. 전환 완료 후 `src/mocks/` 삭제 (파일 상단 주석에 명시)

> 디자인 토큰·화면 레이아웃 기준은 `docs/05-디자인-가이드.md`.
