# 📋 구현 현황 — MLsoft 연차 관리 시스템 V2

> **기준일:** 2026-07-11 (코드 `0cac59f`)
> 이 폴더는 "이때까지 구현된 기능"을 코드 기준으로 점검한 스냅샷입니다. 설계 문서(`docs/01`~`08`)가 "무엇을 만들 것인가"라면, 이 폴더는 "지금 무엇이 실제로 되어 있는가"입니다.

## 한눈에 보기

| 영역 | 상태 | 요약 |
|---|---|---|
| 인증 (OAuth2 + JWT + 온보딩) | ✅ 완료 | Google 로그인 → JWT httpOnly 쿠키 → 온보딩까지 실동작 |
| 백엔드 도메인 모델 (엔티티·리포지토리) | ✅ 완료 | user/department/leave/welfare/holiday/policy/email 전 도메인 |
| **연차 코어 API** (`/api/leaves` 11개) | ✅ 완료 | 신청·승인·취소·요약·캘린더 등 11개 엔드포인트 + 테스트 19건 (전체 29 그린) |
| 기타 업무 API (복리·부서·사용자·이메일) | ❌ 미구현 | 컨트롤러/서비스 없음 — 엔티티만 존재 |
| 초기 데이터 시딩 (DataInitializer) | ✅ 완료 | 연차 정책·시스템 설정·복리후생 정책·미배정 부서 |
| 프론트 인증/앱 셸 (라우팅·레이아웃·가드) | ✅ 완료 | 실 API 연동 |
| 프론트 9개 페이지 | 🟡 목업 UI | 화면은 완성, **데이터는 mock** (실 API 연동은 미착수) |
| 스케줄러 (기산일·생일반차·월차) | ❌ 미구현 | |
| 이메일 발송 (신청/승인/리마인더) | ❌ 미구현 | |
| 로컬 실행 환경 | ✅ 완료 | MySQL96 + local 프로필 + 관리자 부트스트랩 |

**범례:** ✅ 완료 · 🟡 부분(목업/스캐폴딩) · ❌ 미구현

## 07-05 스냅샷 이후 변경 (07-06 ~ 07-07)

- **대시보드 재구성 + 캘린더 드래그 신청 패널** — 캘린더는 팀 캘린더 페이지 전담, 신청은 `LeaveApplyPanel`(플로팅 패널)로. 이후 가독성 타이포 확대.
- **백엔드·프론트 최종 리뷰 결함 수정 (Fable 검증)** — 상세는 [백엔드.md](백엔드.md)·[프론트엔드.md](프론트엔드.md)의 "최종 리뷰 결함 수정" 절 참조.
- **`/dev` 커맨드 추가** — 백엔드(8080)·프론트(5173) 개발 서버 동시 기동 ([실행-환경.md](실행-환경.md)).

## 07-08 스냅샷 이후 변경 (07-10)

- **연차 코어 API 구현** (`0cac59f`) — `LeaveController` 11개 엔드포인트 + `LeaveService` + DTO 7종. 엔티티 변경 없음(기존 상태전이·선차감/복구·`@Version` 재사용), 마이그레이션 없음. 상세는 [백엔드.md](백엔드.md)의 "연차 코어 API" 절.
- **동시성 방어(docs/01 R-5)** — 승인/반려/승인취소는 조건부 갱신(CAS, rowcount 0 → `ALREADY_PROCESSED` 409), 잔여 초과 경쟁은 `User.@Version` 낙관적 락(409 자동).
- **테스트 19건 추가** — `LeaveServiceTest` 18 + `LeaveServiceIntegrationTest`(H2) 1. 전체 29/29 그린, `./gradlew build` 통과.

## 문서 인덱스

- [백엔드.md](백엔드.md) — 구현된 인증·연차 코어 API·시큐리티·도메인 모델, 미구현 API 목록
- [프론트엔드.md](프론트엔드.md) — 9개 페이지·공유 기반·mock 데이터, 실 API 전환 방법
- [실행-환경.md](실행-환경.md) — 로컬 실행·시크릿·관리자 부트스트랩·재기동

## 현재 실행 상태 (2026-07-11)

- 백엔드: `http://localhost:8080` (local 프로필, MySQL96 `mlsoft_leave`)
- 프론트: `http://localhost:5173`
- 개발용 로그인 계정 → **SYSTEM_ADMIN** (관리자 화면 확인 가능). 이메일 값은 `application-local.yml`의 `admin-emails` 참조 — [실행-환경.md](실행-환경.md#관리자-부트스트랩-admin_emails)

## 다음 할 일 (우선순위 제안)

1. **프론트 9개 화면 실 API 연동 + `src/mocks/` 삭제** — 연차 화면(대시보드·캘린더·내역·결재)부터 `/api/leaves` 연동 가능
2. 부서·사용자 관리 API (`/api/departments`, `/api/users`) → 구성원/팀 페이지 실 연동
3. 복리후생 API (`/api/welfare-*`)
4. 스케줄러 (기산일 리셋·생일 반차·월차 적립)
5. 이메일 발송 인프라 (`@Async` + AFTER_COMMIT) + 발송 이력 — 연차 API에 `// TODO(email)` 시밍 존재
6. 프론트 **연차 신청 폼**(디자인 ③) 신설 — 현재 라우트 없음

> 상세 계약은 `docs/03-API-설계.md`, 기능 명세는 `docs/01-요구사항-기획.md` 참조.
