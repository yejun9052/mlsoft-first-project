# 11. 프로젝트 흐름

> 이 문서는 "이 시스템이 어떻게 돌아가는가"를 한 파일로 따라갈 수 있게 정리한 것이다.
> 설계 의도는 docs/01~05, 결함 목록은 docs/07·08·10에 있다. 여기는 **흐름과 구조**를 다룬다.
> 현재 구현 상태와 다음 작업의 단일 원본은 [docs/12-남은-작업.md](12-남은-작업.md)다.
> 최종 실측: 2026-09-09

---

## 1. 한눈에 보기

MLsoft 사내 **연차·복리후생 관리 시스템**. 이전 버전(`참고자료/MLsoft`)을 분석해 재설계한 V2.

| 구분 | 내용 |
|---|---|
| 백엔드 | Spring Boot 4 / Java 21 / JPA(Hibernate) / MySQL 8 |
| 프론트 | React 19 / Vite / Tailwind v4(CSS-first) / React Query / react-router |
| 인증 | Google OAuth2 → **JWT를 HttpOnly 쿠키로** 발급 (세션 없음, STATELESS) |
| 배포 | 루트 `Dockerfile` 3단계 멀티스테이지 → **단일 컨테이너·단일 포트 8080** |
| 규모 | 엔드포인트 **88개**(컨트롤러 18) · 엔티티 **19개**(+ENUM 12, 테이블 21개) · 화면 **17개** |
| 테스트 | 백엔드 **500** · 프론트 **327**(41파일). **mock은 하나도 남지 않았다** — 모든 화면이 실 API를 탄다 |

**핵심 설계 결정 3가지** — 이걸 모르면 코드가 이상하게 보인다.

1. **JWT는 상태를 못 담는다** → 발급 후 퇴직·권한 변경을 반영할 수 없으므로,
   `OnboardingCheckInterceptor`가 `/api/**` 전역에서 **매 요청 DB를 조회**해 다시 검사한다.
2. **연차는 신청 시점에 선차감**한다 → 승인 시가 아니라 `PENDING`이 될 때 `use_days`가 깎인다.
   반려·취소는 "복구" 동작이다.
3. **토큰 이름은 고정, 값만 교체** → 디자인 토큰(`navy-*`/`accent-*`/`ink-*`)은 이름을 유지해서
   페이지에 흩어진 유틸리티 클래스가 팔레트 변경을 자동으로 따라오게 한다.

---

## 2. 요청 하나가 흐르는 길

### 2-1. 로그인 (최초 1회)

```
브라우저
  │ "Google 계정으로 로그인" 클릭
  ▼
GET /oauth2/authorization/google        ← Spring Security가 처리 (컨트롤러 없음)
  │
  ▼ Google 인증 후 콜백
CustomOAuth2UserService
  │ ① 이메일 추출
  │ ② 도메인 검증 (app.allowed-domain)   비어 있으면 검증 안 함(개발용) —
  │                                      운영에서는 AllowedDomainCheck가 기동을 막는다 (O-1)
  │ ③ 없는 계정이면 자동 가입
  │    └ ADMIN_EMAILS에 있으면 SYSTEM_ADMIN 부여 (최초 가입 시에만!)
  ▼
OAuth2SuccessHandler
  │ JWT 생성 → HttpOnly 쿠키로 심음 (COOKIE_SECURE 운영 필수)
  ▼
프론트 /oauth/callback (OAuthCallbackPage)
  │ GET /api/auth/me 로 내 정보 조회 → localStorage.userInfo 저장
  ▼
onboarding_status == COMPLETED? ──아니오──▶ /onboarding (생년월일·입사일 입력)
  │ 예
  ▼
/dashboard
```

### 2-2. 로그인 이후 모든 API 요청

```
프론트 페이지
  │ 예: DashboardPage가 useLeaveSummary() 호출
  ▼
hooks/useLeaves.js          React Query 래퍼 (쿼리 키 = ['leaves', 서브리소스, ...파라미터])
  ▼
api/leaves.js               엔드포인트 1:1 함수, res.data.data 언랩
  ▼
api/index.js                axios 인스턴스 (withCredentials: true)
  │                         └ 응답 인터셉터: 401 → localStorage 정리 + 로그인 리다이렉트
  │                                          그 외 에러 → 서버 메시지를 toast로 일괄 표출
  ▼ (dev: vite 프록시 5173 → 8080 / prod: 같은 오리진)
─────────────────────── 백엔드 ───────────────────────
JwtFilter                   쿠키에서 JWT 읽어 AuthUser principal을 SecurityContext에 세팅
  ▼
SecurityFilterChain         경로별 인증 요구
  ▼
OnboardingCheckInterceptor  ★ /api/** 전역 가드 — 매 요청 DB 조회
  │ ① 퇴직자(is_active=false)      → 로그아웃 외 전 경로 차단
  │ ② DB role ≠ 토큰 role          → SecurityContext 권한을 DB 기준으로 재구성
  │ ③ 온보딩 미완료(onboarding_status != COMPLETED) → /api/auth/* 외 403
  ▼
Controller                  @PreAuthorize는 역할 게이트 전용
  │                         본인 식별은 @AuthenticationPrincipal AuthUser에서만
  ▼
Service                     ★ 소유권·승인자 식별 검증은 여기 책임
  │                         실패는 BusinessException(ErrorCode)
  ▼
Repository → MySQL
  ▲
  │ 예외 발생 시
GlobalExceptionHandler      ErrorCode → HTTP status + 메시지로 일괄 변환
  ▼
CommonResponse<T>           모든 응답이 { success, message, data } 로 래핑
```

**온보딩 완료 판별 주의** — `hire_date`가 채워져 있어도 관리자의 자동 승인 기간 밖이면
`onboarding_status`가 `PENDING`일 수 있다. 따라서 완료 여부는 반드시
`User.isOnboardingCompleted()`로 판단한다. `hire_date != null`을 완료 조건으로 되돌리지 않는다.

**왜 인터셉터가 필요한가** — JWT는 발급 시점의 스냅샷이다. 관리자가 누군가를 팀장으로 승격하거나
퇴직 처리해도 그 사람의 쿠키 안 토큰은 그대로다. 그래서 매 요청 DB로 재확인한다.
대가는 요청당 유저 조회 1회.

---

### 2-3. 이메일 한 통이 나가는 길

```
업무 트랜잭션 (연차 승인 · 온보딩 대기 · 리마인더 잡 …)
  └ EmailNotificationPublisher      수신자 결정 + email_history(PENDING) 생성  ← 같은 트랜잭션
        │                            이벤트에는 id만 담는다
      COMMIT
        ▼
EmailNotificationListener           @Async + @TransactionalEventListener(AFTER_COMMIT)
  └ EmailDeliveryService.send       수신자 1명 = REQUIRES_NEW 트랜잭션
        │  PENDING/FAILED → SENDING 을 조건부 UPDATE로 **선점**
        ▼
     SMTP 발송 ──┬─ 성공 → SENT
                 └─ 실패 → FAILED (retry_count += 1, 사유 500자)
                              │
        EmailRetryScheduler (15분) ─┘  FAILED(retry<3) + 오래 묶인 PENDING 회수
                                       관리자 수동 재발송은 FAILED만 (POST /api/emails/{id}/resend)
```

**이력을 업무 트랜잭션 안에서 만드는 이유** — 업무가 롤백되면 이력도 함께 사라지고(안 보낼 메일이
나가지 않는다), 커밋되면 `PENDING`이 반드시 남는다(리스너가 못 돌아도 스케줄러가 줍는다).

**`SENDING` 선점이 필요한 이유** — 비동기 리스너와 재시도 스케줄러가 같은 이력을 동시에 집으면
수신자가 같은 메일을 두 번 받는다. 상태 전이를 조건부 UPDATE로 만들어 한쪽만 이기게 한다.
다만 복구는 `sending_at`(선점 시각) 기준이며, 선점 소유자를 기록하지 않아 **단일 인스턴스 전제는 여전히 남는다** (docs/12 참고).

---

## 3. 연차 잔액이 변하는 길 ★ 가장 중요

### 3-1. 기본 식

```
잔여(remaining) = base_days + bonus_days − use_days
```

`use_days`는 **현재 회차 창 안의 확정 사용 + 대기 중 선차감**을 모두 포함한다.
다음 회차 예약분은 `use_days`에 넣지 않고 날짜 창 집계로 관리한다.
`pending_days`(현재 회차 창의 PENDING 신청 일수 합)는 `use_days` 안에 이미 들어 있다.

### 3-2. 상태 전이

```
                  ┌──────────────────────────────────────────┐
                  │  신청 (POST /api/leaves)                  │
                  │  ★ 이 시점에 현재 회차분만 use_days 선차감 │
                  │  잔여 부족 + 당겨쓰기 ON이면              │
                  │  부족분이 advance_days에 잡힌다            │
                  │  → LeaveRequest.advanceUsedDays 는 감사용 │
                  └──────────────┬───────────────────────────┘
                                 ▼
                            ┌─────────┐
                            │ PENDING │
                            └────┬────┘
              승인 ┌─────────────┼─────────────┐ 반려
                   ▼             │             ▼
             ┌──────────┐        │       ┌──────────┐
             │ APPROVED │        │       │ REJECTED │
             └────┬─────┘        │       └──────────┘
                  │              │        use_days 복구
       본인 취소 신청             │        → advance_days 는
                  │           본인 취소    재계산으로 따라옴
     ┌────────────┴───────┐      ▼
     │ 미래 날짜만 포함    │  ┌───────────┐
     │ → 즉시 CANCELLED   │  │ CANCELLED │
     │                    │  └───────────┘
     │ 과거 포함(소급)     │
     │ → CANCEL_PENDING   │
     └────────┬───────────┘
              ▼
      ┌────────────────┐
      │ CANCEL_PENDING │ ← 결재자가 승인/거부
      └────────────────┘
         승인 → CANCELLED (복구)
         거부 → APPROVED 로 되돌림
```

**`advance_days`는 파생값이다** — 되돌리는 게 아니라 매번 다시 계산한다 (2026-08-06, 리뷰 I-1).

```
advance_days = max(0, use_days − base_days − bonus_days)
```

`User.syncAdvanceDays()` **하나만** 이 필드에 쓰고, 잔액 3필드를 바꾸는 도메인 메서드 6개가
(`deductLeave`·`restoreLeave`·`addBonusDays`·`updateBaseDays`·`addMonthlyLeave`·`resetAnnualLeave`)
마지막에 이걸 호출한다. **예외 없다.**

왜 이렇게 됐나 — 예전에는 신청별 `advanceUsedDays` 스냅샷을 빼서 복구했는데, 그러면
**취소 순서에 따라 결과가 달라졌다.** A(8일)·B(5일)를 신청하고 A를 먼저 취소하면 B는 잔여로
충당되는데도 당겨쓰기 3일이 남아, 다음 기산일에 쓰지 않은 3일이 삭감됐다.
보너스 가산·관리자 연차 변경·월차 적립도 같은 이유로 정산이 누락됐다 (I-1·I-2·I-8이 한 원인).

`LeaveRequest.advanceUsedDays`는 **감사 기록으로만** 남는다 — 복구 계산에는 쓰이지 않는다.

`RequestStatus`(PENDING/APPROVED/REJECTED/CANCELLED/CANCEL_PENDING)는 연차·복리후생이 공유한다.
취소 승인/거부의 **세부 결과는 status가 아니라** `action_history`의 `RequestAction`으로 기록한다.

### 3-3. 동시성

`User`에 `@Version` 낙관적 락. 두 창에서 동시 신청해도 초과 차감되지 않는다.
상태 전이는 전부 도메인 메서드(`deductLeave` / `restoreLeave` / `resetAnnualLeave`) 안에 있고
**Setter는 없다.**

### 3-4. 규칙

- 연차 일수는 **`BigDecimal`만** (double 금지, DB는 `DECIMAL(4,1)`)
- 비교는 `compareTo`, **`==` 금지**

---

## 4. 기능별 구현 상태 — 역사 요약

> 이 절의 표는 2026-08-07 전후의 흐름을 설명하기 위한 역사 요약이다.
> 최신 구현 여부·완료/부분/미구현 판정과 우선순위는 [docs/12-남은-작업.md](12-남은-작업.md)의
> **현재 상태 스냅샷**과 열린 작업 표를 기준으로 한다. 이 문서와 docs/12에 상태표를 각각 추가하지 않는다.

### ✅ 돌아가는 것

| 기능 | 비고 |
|---|---|
| Google OAuth2 로그인 + 온보딩 | 자동 가입, 도메인 검증 |
| 연차 신청·승인·반려·취소 (백엔드) | 13 엔드포인트, 선차감·복구·당겨쓰기.<br>잔액 불변식은 파생값 재계산으로 통합 (§3-2, 리뷰 I-1·I-2·I-8 해결) |
| 복리후생 신청·승인 | 6 엔드포인트 + 정책 CRUD 6 |
| 결재 관리 화면 | 대기 목록, 승인/반려 |
| 구성원 관리 · 부서 관리 | CRUD + 팀장 지정, 퇴직 처리(결재 이관), **퇴직 복구**(2026-08-16).<br>부서·역할은 **표에서 바로** 고친다. **역할이 단일 출처**라 팀장으로 올리면 그 부서 `leader_id`까지<br>함께 채워지고 기존 팀장은 사원으로 내려간다 — 부서당 팀장 1명 (docs/01 §2-9(b)) |
| 연차 정책 관리 | 근속년수별 정책 CRUD + **시스템 설정 9키** (타입·범위 검증, 서버가 메타데이터까지 내려줌 — docs/02 3-11) |
| **이메일 건별 알림** | 신청·승인·반려·취소·소급취소·복리후생·생일 반차. 수신자는 신청자 + primary + sub<br>(결과 메일은 SYSTEM_ADMIN 참조). 본문은 **HTML 표 + 바로가기 버튼**(`app.frontend-url` 기준).<br>발송 실패는 업무를 실패시키지 않고 `email_history`에 남아 15분마다 재시도(최대 3회) |
| **이메일 2단계 — 관리자 발송·양식·연동** | 소진 안내 대상 조회·일괄 발송(회당 100·일 400)·발송 이력·FAILED 재발송,<br>양식 편집과 미리보기, 메일 계정·공휴일 키를 암호화 저장하는 외부 연동 설정.<br>화면은 `/admin/emails`·`/admin/integrations` 2개이고 **둘 다 SYSTEM_ADMIN 전용**이며<br>자격 증명은 마스킹된 값만 응답에 담는다. `reminder_*` 2키는 ACTIVE로 전환했다 (2026-09-05) |
| **온보딩 승인 알림** | 대기 발생은 재직 SYSTEM_ADMIN 전원, 승인·반려는 사원 본인, 1회 수정은 총관리자.<br>`EmailType.NOTICE`를 재사용해 ENUM 확장 없이 붙였다 |
| **승인자 상위 부서 승격** | 소속 부서 팀장이 없거나 자격을 잃으면 **상위 부서 팀장** → 그마저 없으면 SYSTEM_ADMIN.<br>판정 규칙 전문은 docs/01 §2-3(a) |
| 복리후생 정책 관리 UI | `AdminWelfarePoliciesPage` — 백엔드 CRUD와 연결 완료 |
| 보안 테스트 | `SecurityAccessMatrixTest` — 실제 JWT 쿠키로 필터 체인·인터셉터·`@PreAuthorize`·소유권을 한 요청에 통과시킨다 |
| 처리 이력 로그 | 6 엔드포인트, 팀 스코프를 **서버가** 결정, `@EntityGraph`로 N+1 차단 |
| **N+1 방지 (전 목록)** | 2026-08-17 감사에서 **미해결 0건** 확인. 목록 조회는 to-one `@EntityGraph`,<br>`LeaveRequest.dates`·`ScheduleEntry.dates`는 `@BatchSize(50)`으로 분리했다 —<br>컬렉션을 `@EntityGraph`에 넣고 페이징하면 Hibernate가 전체를 메모리로 올린다(`HHH90003004`) |
| **목록 페이지네이션** | 데이터가 많은 표 7곳. 서버로 끊는 곳(구성원 3탭·복리후생 내역·리셋 이력·처리 이력·복리후생 정책)과<br>**화면에서 끊는 곳**(내 연차 내역·결재 관리)이 갈린다 — 앞은 화면 필터가 없고, 뒤는 화면에서<br>거르거나(연도·종류·상태) 두 목록을 합쳐 정렬해서 서버가 먼저 끊으면 결과가 틀어진다.<br>목록이 줄면 `usePageClamp`이 마지막 페이지로 당긴다 |
| **결재 자기 승인 금지** | 신청자 본인은 자기 건을 결재할 수 없다. 승인자 후보 목록에서 빼는 것과 별개로<br>`validateApprover`가 결재 시점에 막는다 (2026-08-17).<br>**총관리자만 예외다** (2026-08-19) — 위에 결재선이 없어 막으면 신청 자체가 `INVALID_APPROVER`로<br>실패한다. `ApproverResolver`가 총관리자를 본인의 승인자로 배정한다.<br>푸는 것은 자기 것뿐이라 **배정되지 않은 남의 건은 총관리자도 결재하지 못한다** |
| **저장 안 한 변경 붙잡기** | 연차 정책의 시스템 설정은 **따로 "설정 저장"을 눌러야** 반영된다.<br>저장 버튼 옆에 미저장 건수 배지를 띄우고, `useUnsavedGuard`가 **링크 클릭(캡처 단계)**과<br>`beforeunload`를 잡아 확인 창을 띄운다. **뒤로가기는 일부러 막지 않았다** —<br>`BrowserRouter`라 `useBlocker`를 못 쓰고, 우회책은 저장 후에도 뒤로가기를 한 번 먹통으로 만든다 (2026-08-19) |
| 대시보드 · 팀 캘린더 · 사용 내역 · 내 정보 | 실 API 연동 완료 |
| 연차 취소 (프론트) | 사용 내역에서 취소 신청. 과거 날짜 포함 승인 건은 소급 취소로 안내 (F-3) |
| 결재 "처리 완료" 탭 | `GET /api/leave-histories/my-actions` — actor 기준 실 API (F-2) |
| **개인 일정 (외근·출장·재택·교육)** | `domain/schedule` 6 엔드포인트. **연차 차감 없음 + 결재 없음 + 상태 전이 없음**이라<br>별도 도메인으로 뒀다 — `LeaveRequest`에 얹으면 흐름마다 "차감 안 하면 건너뛰기" 분기가 생긴다.<br>캘린더에서 연차와 같은 패널로 등록하고(모드 토글), 주말·과거도 허용한다 |
| 캘린더 이름·부서 검색 | 연차·일정 양쪽에 `keyword`(이름 부분일치)·`departmentId` 필터.<br>캘린더가 이미 전사 공개라 이름 필터가 노출 범위를 넓히지 않는다 |
| 공휴일 | `GET /api/holidays` — data.go.kr 연 1회 조회 + DB 캐시. 연차 신청 시 공휴일 거부(I-6).<br>**조회 실패가 예외가 되지 않는다** — 못 받으면 표시·검증만 빠지고 신청은 계속된다 |
| 시연 데이터 | `DemoDataInitializer`가 local 프로필에서 사원 8·부서 3·신청 8·이력을 **DB에 실제로** 생성.<br>**mock은 전부 사라졌다** (`mocks/data.js` 삭제) |
| **스케줄러 5잡** | `LeaveScheduler`가 매일 00:10 KST에 ① 기산일 리셋 → ② 월차 적립 → ③ 생일 반차 → ④ 연차 소진 안내 → ⑤ 퇴직자 파기.<br>④·⑤가 맨 뒤인 것도 순서 이유가 같다 — 앞 잡이 잔액·안내를 갱신하거나 퇴직자 입력을 바꾸므로 마지막에 실행해야 로그를 해석하기 쉽다.<br>**순서는 데이터 의존성이다** — 리셋이 `bonus_days`를 갈아 끼우므로 생일 반차가 먼저 지급되면 그날 증발하고,<br>월차가 먼저면 1주년에 하루짜리 유령 적립이 남는다. ⑤는 `AUTO`에서만 30일 예고 후 파기·결과 NOTICE를 보내고 MANUAL이면 한 줄 로그로 끝낸다.<br>서버가 꺼져 있어도 catch-up으로 소급하고(리셋은 회차별 while), **사원 1명 = 1트랜잭션**이라<br>낙관적 락 충돌이 나도 그 사람만 건너뛴다(다음 실행에서 자동 복구).<br>매년 1/1에는 공휴일 자동 동기화도 함께 돈다 |

### ⚠️ 반쪽

| 기능 | 무엇이 빠졌나 |
|---|---|
| 개인 일정 수정 | 등록·삭제는 되고 수정 UI가 없다 |

### ❌ 코드가 아예 없는 것

| 기능 | 현재 상태 |
|---|---|
| 퇴직자 3년 경과 후 처분 | 수동·자동 파기와 익명화·30일 예고까지 백엔드 구현 완료. 운영 프로필에서 AUTO를 켜기 전까지는 MANUAL 기본값으로 동작하지 않는다 |
| 스케줄러 수동 트리거 | **확인 수단이 없다** — 손으로 검증하려면 시스템 시각이나 DB를 건드려야 한다 |
| **직책(`position`) 입력 경로** | 컬럼·응답·화면 표시는 있는데 **값을 넣는 곳이 시연 데이터뿐**이다.<br>실제 사원은 전부 `null`. 온보딩·내 정보·관리자 어디에도 입력란이 없다.<br>지금은 어떤 로직에도 쓰이지 않는 표시용 라벨이다 (2026-08-16 확인) |

### 🔴 알려진 결함

| 결함 | 내용 |
|---|---|
| **팀장 교체·강등 동시성** | ① 공석 부서에 두 관리자가 동시에 승격하면 역할이 팀장인 사람이 2명 남는다(`Department`에 `@Version` 없음).<br>② 강등과 신규 신청이 겹치면 결재 불가능한 사람이 승인자인 PENDING이 생긴다.<br>둘 다 스키마·트랜잭션 경계를 건드려야 한다 — docs/12 §④ `SEC-9` (구 S-9, 2026-08-17 감사) |

> **닫힌 결함** — 자기 자신 퇴사(S-7)는 2026-08-17에 서버 `CANNOT_RETIRE_SELF` + 본인 행 버튼 비활성으로,
> 팀장 부서 이동(S-8)은 2026-09-09에 `leader_id` 기준 이동 거부 + 팀장→사원 동시 이동의 공석 처리로 해결됐다.

---

## 5. 데이터 흐름 규칙 (프론트)

```
api/*.js        엔드포인트 1:1 함수. res.data.data 언랩. 여기서 toast 띄우지 말 것
   ▼
hooks/use*.js   React Query 래퍼. 쿼리 키 = ['leaves', 서브리소스, ...파라미터]
   ▼            → 접두사로 일괄 invalidate 가능
페이지
```

- **에러 toast는 `api/index.js` 인터셉터가 일괄 처리** — 개별 호출부에서 중복으로 띄우지 말 것
- 권한 가드는 `RequireAuth`(라우트 레벨)로 선차단. **useEffect 내 리다이렉트 금지**(깜박임·우회)
- React 컴포넌트는 `export default function Name({ props })` — 화살표 함수 컴포넌트 금지

---

## 6. 스타일 (Tailwind v4 CSS-first)

**`tailwind.config.js`는 없다.** 토큰은 `frontend/src/index.css`의 `@theme`, 커스텀 유틸리티는 `@utility`.
값의 단일 출처는 `index.css`이고 docs/05가 그걸 문서화한다.

핵심 규칙:
- **그라데이션 유틸리티는 현재 사용하지 않는다.** 개편 당시의 그라데이션 면도 제거됐으므로
  색상·강조는 `frontend/src/index.css`와 [docs/05-디자인-가이드](05-디자인-가이드.md)를 기준으로 한다.

---

## 7. 로컬 실행

```powershell
# 백엔드 (MySQL96 서비스가 Running이어야 함)
$env:SPRING_PROFILES_ACTIVE = 'local'; .\gradlew.bat bootRun   # 8080

# 프론트
npm run dev                                                     # 5173 (/api·/oauth2를 8080으로 프록시)
```

`/dev` 슬래시 커맨드가 둘 다 띄운다. **서버를 도구의 `run_in_background`로 띄우면 안 된다** —
세션 하네스가 백그라운드 작업을 정리할 때 서버까지 죽는다. `Start-Process`로 분리할 것.

| 파일 | 커밋 | 도커 컨텍스트 | 내용 |
|---|---|---|---|
| `application.yml` | ✅ | 포함 | 공통 설정, 시크릿은 `${ENV}` 참조만 |
| `application-local.yml` | ❌ gitignored | **❌ dockerignored** | 로컬 실제 시크릿 |
| `application-example.yml` | ✅ | 포함 | 템플릿 |

**시크릿은 두 곳에서 따로 막아야 한다** (리뷰 O-4, 2026-08-07 해소).
`.gitignore`에 있다고 도커 이미지에서 빠지는 게 아니다 — `Dockerfile:20`의 `COPY backend/ ./`는
git을 거치지 않고 파일시스템을 그대로 복사하므로, `bootJar`가 `application-local.yml`을
jar 리소스로 패키징해 **운영 이미지 안에 시크릿이 박힌다.** 이미지를 받은 사람은 jar만 풀면 다 읽는다.
지금은 `.dockerignore`가 막고 있다. **새 시크릿 파일을 만들면 `.gitignore`와 `.dockerignore` 둘 다** 넣을 것.

`ADMIN_EMAILS`는 **최초 자동 가입 시에만** SYSTEM_ADMIN을 부여한다.
이미 가입된 계정 승격은 `UPDATE users SET role='SYSTEM_ADMIN'` + **재로그인**이 함께 필요하다.

---

## 8. 문서 지도

| 문서 | 언제 보나 |
|---|---|
| `01-요구사항-기획` | 기능을 새로 만들 때 |
| `02-DB-설계` | 테이블·ENUM 확인 |
| `03-API-설계` | 엔드포인트 추가 시 (응답 포맷·페이징·에러 코드 규칙) |
| `04-코드-스타일-가이드` | 코드 작성 규칙 원본 |
| `05-디자인-가이드` | 토큰·화면 구조·글래스 규칙 |
| `07-기능-갭분석` | 코드 주석의 `갭분석 A-1` 참조처 |
| `08-운영-검증-리포트` | 코드 주석의 `검증 R-5`, `Y-2` 참조처 |
| `09-스케줄러-설계` | 스케줄러 구현 시 (§11에 실행 순서) |
| `10-코드리뷰-리포트` | 코드 주석의 `리뷰 I-1`, `F-3` 참조처 |
| **`11-프로젝트-흐름`** | 이 문서 — 전체 흐름과 구조 |
| **`12-남은-작업`** | 현재 구현 상태와 다음 작업의 단일 원본 — 우선순위·의존 관계·착수 순서 |
| ~~`구현-현황/`~~ | **폐기 (2026-08-07)** — 이 문서 §4와 docs/12가 대체 |

코드 주석의 `(검증 Y-2)`, `(갭분석 A-3)`, `(리뷰 I-1)` 표기는 각 문서의 항목 번호를 가리킨다.
그 로직을 수정할 땐 해당 항목을 먼저 읽을 것.

**리포트 문서(07·08·10)는 본문을 고쳐 쓰지 않는다** — 번호가 바뀌면 코드 주석의 참조가 끊긴다.
07·08은 상단에 "구현 상태" 표를 두어 현재 상태만 갱신한다.
