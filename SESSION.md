---
type: session-pointer
project: MLsoft 휴가관리 V2
obsidian_note: "2026-09-05 이메일 2단계 완료·공휴일 마무리"
updated: 2026-09-05
---
# 2026-09-05 이메일 2단계 완료·공휴일 마무리

> 📌 옵시디언 원본: `06 Claude Code Sessions/2026-09-05 이메일 2단계 완료·공휴일 마무리.md` ([[2026-09-05 이메일 2단계 완료·공휴일 마무리]])
> 프로젝트 전체 진행도 메모리: [`PROJECT_PROGRESS.md`](PROJECT_PROGRESS.md)

## 이번 세션 결과

- 이메일 2단계 W1~W4를 끝냈다 — 테이블 3개·ENUM 확장 2건과 `SchemaEnumConsistencyTest`, `SecretCipher` 일반화와 메일 계정 DB 저장, `SENDING` 선점, 온보딩 알림 4지점, 리마인더 ④잡, 관리자 API 13개(이메일 4·양식 3·연동 6).
- 관리자 화면 2개(`/admin/emails`·`/admin/integrations`)를 붙였다. 둘 다 SYSTEM_ADMIN 전용이고 자격 증명은 마스킹된 값만 응답에 담는다.
- 공휴일을 마무리했다 — 관리자 수동 동기화 UI와 외부 조회·저장 트랜잭션 분리. 정책 카탈로그는 ACTIVE 9개·PENDING_FEATURE 0개가 됐다.
- 취약점 패치와 `CalendarPage` 테스트 시간 고정을 함께 반영했다. 검증은 백엔드 410건 · 프론트 39파일 254건 · lint 0 · build 정상 · npm audit 0.
- `CLAUDE.md`·`docs/11`·`docs/12`·`docs/13`·`PROJECT_PROGRESS.md`를 오늘 상태에 맞게 정리했다. 운영 대상 backfill은 13개가 됐다(`backfill-2026-09-05-email-phase2.sql` 추가).

## 2026-09-06 다음 실행 순서

1. staging 시드를 실행한다 — `APP_CREDENTIAL_ENCRYPTION_KEY` 주입 후 공휴일 키와 메일 계정을 각각 저장한다.
2. 이메일·연동 브라우저 QA(`docs/13` U절)와 공휴일 QA(N절)를 진행하고 결과만 문서에 회수한다.
3. ~~컨트롤러 계층 테스트 보강~~ 완료(2026-09-07). `SENDING` 복구·공휴일 0건 구분도 함께 닫았다.
4. 운영 DB 백업·preflight·backfill 15개·`ddl-auto=validate` 기동·smoke test.

## 이월 작업 (이메일 2단계에서 남긴 것)

- **(a) staging 시드 실행** — 공휴일 키·메일 계정. `APP_CREDENTIAL_ENCRYPTION_KEY`가 없으면 화면이 경고를 띄우고 저장이 잠긴다.
- **(b) 이메일·연동 브라우저 QA** — 실제 발송이 걸려 있어 자동 테스트로 대체할 수 없다 (`docs/13` U-1~U-12).
- ~~**(c) 컨트롤러 테스트 보강**~~ 완료(2026-09-07) — 양식·연동 테스트 13건 + 실제 필터 체인 권한 검사.
- ~~**(d) `SENDING` 복구 기준**~~ `sending_at` 기준으로 교체(2026-09-07). 선점 소유자는 여전히 기록하지 않아 **단일 인스턴스 전제는 남는다**.
- ~~**(e) 0건 공휴일 응답**~~ 완료(2026-09-07) — 정상 0건과 키 없음·호출 실패·이상 응답을 구분해 관리자 경로에서만 오류로 표면화한다.

## 2026-09-07 추가 작업

- 남은 코드 작업 3건을 닫았다 — 관리자 컨트롤러 테스트, `SENDING` 복구를 선점 시각 기준으로, 공휴일 0건과 장애 구분.
- 문서 전수 감사로 수치·상태 모순을 정정했다(스케줄러 잡 3→4, 테이블 20, 엔티티 18 등).
- **미래 차감 연차 분리**를 설계하고 구현했다 — 다음 기산일 이후 날짜는 현재 회차 잔액에서 빼지 않는다. 회차 창을 신청·복구·리셋이 공유하고, 리셋 채무 식에서 `carriedUse` 뺄셈을 걷어냈다(검산은 설계 §3). 브라우저 확인은 `docs/13` V절, 기존 데이터는 backfill 15번.

## 미해결

- 미래 차감 연차, 관리자 문의, 미배정 부서 팀장 정책, S-8 팀장 부서 이동, S-9 동시성.
- 모바일 정책·부서 관리 터치, 다중 날짜 선택·신청 화면 복귀, 키보드 월 이동.
- Google OAuth 테스트 계정·staging DB 권한이 없어 사용자 QA·운영 preflight는 별도 실행해야 한다.

## 이 프로젝트 세션 (최근순)

- [[2026-09-05 이메일 2단계 완료·공휴일 마무리]] — 이메일 2단계 W1~W4와 공휴일 마무리, 관리자 화면 2개를 반영했다.
- [[2026-08-25 모바일 캘린더 하단 상세·내일 작업 계획]] — 모바일 캘린더 변경과 내일 공휴일·이메일·QA 순서를 기록했다.
- [[2026-08-24 진행도 메모리·Claude 잔여 기능·전체 변경 커밋]] — 진행도 메모리와 Claude/Opus 세션 잔여 기능을 통합했다.
- [[2026-08-23 Codex·Opus QA 재개·프리플라이트·커스텀 명령어]] — 커스텀 명령·QA 작업지·운영 preflight를 정리했다.
- [[2026-08-22 1차 점검 반영·데이터 라우터 전환·모바일 1단계·연차 히트맵]] — 1차 점검·데이터 라우터·모바일 1단계·연차 히트맵을 반영했다.
- [[2026-08-16 수동 점검 착수 — 점검 중 나온 결함을 그 자리에서 수정]] — 수동 점검 중 발견한 결함과 정책을 수정했다.
- [[2026-08-11 이메일 제외 잔여 작업 전량 완료 + Codex 위임 분업 확립]] — 이메일을 제외한 잔여 작업과 Codex/Claude 분업을 정리했다.
- [[2026-08-07 코드리뷰 1~3순위 해소 + 남은 작업 문서화]] — 코드리뷰 상위 항목과 남은 작업을 문서화했다.
- [[2026-07-30 관리자 부서 관리 UI + 처리 이력 로그 신규 구현]] — 부서 관리 UI와 처리 이력 로그를 구현했다.
