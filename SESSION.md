---
type: session-pointer
project: MLsoft 휴가관리 V2
obsidian_note: "2026-09-08 미래 회차 분리·퇴직자 파기·재입사·직급"
updated: 2026-09-08
---
# 2026-09-08 미래 회차 분리 · 퇴직자 파기 · 재입사 · 직급

> 프로젝트 전체 진행도: [`PROJECT_PROGRESS.md`](PROJECT_PROGRESS.md)
> 기능별 현재 상태의 정본: [`docs/12-남은-작업.md`](docs/12-남은-작업.md)
> **다음에 할 QA:** [`docs/설계-초안/사용자-QA-마무리-2026-09-08.md`](docs/설계-초안/사용자-QA-마무리-2026-09-08.md)
> (같은 내용을 캡처와 함께 아티팩트로도 공유했다 — 링크는 옵시디언 세션 노트에)

## 지금 상태 한 줄

배포 전 기능은 사실상 다 들어갔다. **남은 것은 사람이 브라우저와 운영 DB에서 해야 하는 일**이다.

| | |
|---|---|
| 자동 검증 | 백엔드 500 · 프론트 332(41파일) · lint 0 · build 정상 · npm audit 0 |
| 코드 규모 | 컨트롤러 18(엔드포인트 88) · 엔티티 19 · 테이블 21 · 화면 17 |
| 정책 카탈로그 | ACTIVE 12 · PENDING_FEATURE 0 |
| 운영 대상 backfill | 18개 (아직 하나도 실행하지 않았다) |
| 원격 | 2026-09-09 푸시 완료 |

## 2026-09-08에 넣은 것

1. **미래 차감 연차 분리** — 다음 기산일 이후 날짜는 현재 회차 잔액에서 빼지 않는다.
   회차 창 `[기산일, +1년)`을 신청·복구·리셋이 공유하고, 리셋 채무 식에서 `carriedUse` 뺄셈을 걷어냈다.
   설계·검산 `docs/설계-초안/미래-차감-연차-분리-설계-2026-09-07.md`, 보정 backfill 15번.
2. **퇴직자 데이터 파기** — 정책으로 자동/수동을 고르고, 파기는 익명화 + 자유 텍스트 본문까지.
   자동 모드는 스케줄러 ⑤로 30일 예고 후 실행한다. 기본이 `MANUAL`이라 켜기 전에는 아무것도 사라지지 않는다.
   backfill 16번.
3. **재입사자 처리** — 근속이 끊기면 연차를 0부터. `last_reset_date`를 재입사일로 세워 공백 catch-up을 막고,
   이전 근속의 대기 신청을 종결시킨다. 지나간 근속은 `employment_periods`에. backfill 17번.
4. **직급 자가 입력** — `users.job_grade` 신규. 온보딩(선택)과 내 정보에서 사원이 직접 넣는다.
   기존 `position`(직책)은 그대로 둔다. backfill 18번.
5. **남아 있던 코드 부채 3건** — 관리자 컨트롤러 테스트, `SENDING` 복구를 선점 시각 기준으로,
   공휴일 0건과 장애 구분.
6. **캘린더 키보드 월 이동**과 **자기 퇴직 가드 회귀 테스트**.
7. **문서 전수 감사** — 수치·상태 모순을 실측으로 맞췄다(스케줄러 잡 3→4→5, 테이블 20→21 등).

## 다음에 할 일 — 순서대로

1. ~~브라우저 QA C-1~C-13~~ **완료(2026-09-09)** — 에이전트가 로컬 서버를 띄워 23개 항목을 전부 돌렸다.
   결과는 `docs/13` V·W절에 있다. 남은 것은 실제 메일이 나가는 U절과 공휴일 N절이다.
2. **정책 결정 5건** — 관리자 문의, 미배정 부서 팀장, `SEC-8`, `SEC-9`, 모바일 3건.
3. **staging 시드** — `APP_CREDENTIAL_ENCRYPTION_KEY` 주입 후 공휴일 키·메일 계정 저장.
4. **운영 DB** — 백업 → preflight → backfill 18개(`docs/12` 맨 위 표 순서) → `validate` 기동 → smoke test.

## 열려 있는 것

**코드에서 바로 판단할 수 있는 것**

- ~~B-1 다른 달의 주말이 선택돼 있으면 주말 경고가 사라진다~~ **해결(2026-09-09)** —
  주말 판정을 패널이 날짜에서 직접 하도록 옮기고, 주말·공휴일이 섞이면 신청 버튼도 잠근다.
- ~~표시 결함 3건(B-2~B-4)~~ **해결(2026-09-09)** — 관리 열 고정, placeholder 정정, 재입사 적용 결과에 직급 추가 + 바뀌지 않는 행 숨김.

**코드로 열린 결함은 없다.**

**정책 결정이 필요한 것**

- 관리자 문의 기능, 미배정 부서 자체의 팀장 허용 여부, `SEC-9` 팀장 교체 동시성.
- 모바일 3건은 2026-09-09~10에 모두 완료했다. 실제 폰 확인은 `docs/13` X절.
- `SEC-8`은 2026-09-09에 (c) 거부로 확정·구현했다.

**환경이 있어야 하는 것**

- Google OAuth 계정이 필요한 사용자 QA, staging·운영 DB 접속이 필요한 preflight·backfill.
- `SENDING` 복구는 선점 소유자를 기록하지 않아 **단일 인스턴스 전제**다. 인스턴스를 늘리기 전에 다시 설계한다.

## 이 프로젝트 세션 (최근순)

- [[2026-09-08 미래 회차 분리·퇴직자 파기·재입사·직급]] — 설계 3건을 쓰고 순서대로 구현한 뒤 브라우저로 확인했다.
- [[2026-09-05 이메일 2단계 완료·공휴일 마무리]] — 이메일 2단계 W1~W4와 공휴일 마무리, 관리자 화면 2개를 반영했다.
- [[2026-08-25 모바일 캘린더 하단 상세·내일 작업 계획]] — 모바일 캘린더 변경과 내일 공휴일·이메일·QA 순서를 기록했다.
- [[2026-08-24 진행도 메모리·Claude 잔여 기능·전체 변경 커밋]] — 진행도 메모리와 Claude/Opus 세션 잔여 기능을 통합했다.
- [[2026-08-23 Codex·Opus QA 재개·프리플라이트·커스텀 명령어]] — 커스텀 명령·QA 작업지·운영 preflight를 정리했다.
- [[2026-08-22 1차 점검 반영·데이터 라우터 전환·모바일 1단계·연차 히트맵]] — 1차 점검·데이터 라우터·모바일 1단계·연차 히트맵을 반영했다.
- [[2026-08-16 수동 점검 착수 — 점검 중 나온 결함을 그 자리에서 수정]] — 수동 점검 중 발견한 결함과 정책을 수정했다.
- [[2026-08-11 이메일 제외 잔여 작업 전량 완료 + Codex 위임 분업 확립]] — 이메일을 제외한 잔여 작업과 Codex/Claude 분업을 정리했다.
- [[2026-08-07 코드리뷰 1~3순위 해소 + 남은 작업 문서화]] — 코드리뷰 상위 항목과 남은 작업을 문서화했다.
- [[2026-07-30 관리자 부서 관리 UI + 처리 이력 로그 신규 구현]] — 부서 관리 UI와 처리 이력 로그를 구현했다.
