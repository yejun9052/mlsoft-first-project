# MLsoft 휴가관리 V2 — 진행도 메모리

> 기준일: 2026-09-05 (KST)
> 브랜치: `main`
> 이 파일은 세션이 바뀌어도 지금까지의 진행 맥락을 복구하기 위한 인수인계용 메모리다. 기능별 현재 상태의 정본은 [`docs/12-남은-작업.md`](docs/12-남은-작업.md), 시스템 흐름은 [`docs/11-프로젝트-흐름.md`](docs/11-프로젝트-흐름.md), 수동 QA 절차는 [`docs/13-QA-체크리스트.md`](docs/13-QA-체크리스트.md)다.

## 1. 현재 기준

- 현재 코드 기준 테스트 수치: 백엔드 439건, 프론트 254건.
- 코드 규모: 컨트롤러 18개(엔드포인트 83개), 엔티티 18개(+ENUM 12), `db/schema.sql` 테이블 20개, 화면 17개.
- 마지막 자동 검증은 2026-09-07에 수행했다: 백엔드 439건, 프론트 전체 39개 테스트 파일·254건 통과, lint 경고 0, build 성공(vendor 청크 분리, npm audit 0건).
  `/dev` 서버는 이번 기록 작업에서 기동하지 않았고 운영 DB preflight도 실행하지 않았다.
- 운영 DB preflight·backfill 14개·`ddl-auto=validate` 기동 검증은 아직 실제 staging에서 실행하지 않았다.
- 비밀 파일(`application-local.yml`, `.env.prod`, `*.key`, `*.pem`)은 열거나 커밋하지 않는다.

## 2. 지금까지 완료한 구현·검증

### 기능 구현

- 연차 잔액 불변식과 `advance_days` 파생값 통합, 당겨쓰기 상한·정책 설정 카탈로그 반영.
- 연차 신청·승인·반려·취소·복리후생·결재자 결정·권한/온보딩 가드·감사 로그 구현.
- 스케줄러 4개(기산일 리셋·월차 적립·생일 반차·연차 소진 안내)와 catch-up 로직 구현.
- 공휴일 API/캐시, 개인 일정, 캘린더·대시보드 데이터 흐름 구현.
- 공휴일 API 테스트 호출 완료. API 키는 제공자별 AES-GCM 암호문으로 별도 저장하고, `holidays`의 날짜·명칭은 캘린더의 `HOLIDAY` 일정 이벤트로 합치는 경계를 추가했다. API `resultCode` 검증과 파서 회귀 테스트도 추가했다. 운영 DB 시드는 `HOLIDAY_CREDENTIAL_SEED=true`인 1회 작업으로 제한하고, 키 회전 UI는 남아 있다.
- 이메일 건별 알림, 커밋 후 비동기 발송, `PENDING/SENT/FAILED` 이력과 실패 재시도 구현.
- 연차 히트맵, 직책 자가 설정, 퇴직 경과일, 긴 사유 상세 모달, 캘린더 명칭·월 이동 버튼, 정책 미저장 경고 구현.
- 미배정 부서의 구조 식별과 기본 부서 잠금 반영.
- E-1 화면 표시 수정: `DepartmentResponse.unassigned`를 사용해 실제 부서의 팀장 미지정과 부서 미배정을 분리하고 회귀 테스트 추가.
- 모바일 캘린더 2단계 반영: 안드로이드 캘린더형 정사각 셀, 인접 월 날짜, 좌우 스와이프 월 이동, 선택 날짜 하단 일정 요약·추가 버튼, 일정 종류별 색 점, 모바일 등록 하단 시트. 모바일 셀의 `...더보기`는 제거하고 데스크톱에만 유지한다.

### QA·운영 문서

- 기존 Claude 세션과 1차/2차 QA 결과를 Codex 세션에 인수인계.
- 사용자 직접 QA I-1~I-4 작업지 작성 및 `docs/13`에 QA 항목 회수.
- Claude 호스팅 인터랙티브 QA 워크시트 발행:
  - Artifact: `https://claude.ai/code/artifact/4c3f2f1d-186b-4970-a3b1-866bc1f55b36`
  - 로컬 보조 주소: `http://127.0.0.1:4821/onboarding-qa-worksheet.html` (호스팅 프로세스가 살아 있을 때만 유효)
  - PASS/FAIL/UNKNOWN, 피드백·증거 메모, localStorage 복원, Markdown/JSON 내보내기 지원.
  - 실제 앱 화면 캡처는 Google OAuth 테스트 계정 부재로 차단됐고, 예시 일러스트와 실제 인증 벽 캡처를 분리했다.
- 읽기 전용 운영 DB preflight SQL 작성 및 Opus 재감사 반영.
- Opus 하위 워크스페이스의 문서 중복·코드/문서 정합성·프론트/백엔드 잔여 기능 감사를 반영.
- MD exact duplicate 0건, 내부 Markdown 링크 오류 0건 확인.
- 문서 역할 통합:
  - `docs/11`: 구조·흐름
  - `docs/12`: 현재 상태·남은 작업 단일 원본
  - `docs/13`: 수동 QA 원본
  - `docs/설계-초안/README.md`: 날짜가 붙은 감사·QA·preflight 기록 색인
- 온보딩 판별을 `hire_date`가 아니라 `onboarding_status == COMPLETED`로 통일.
- 월차 설계를 `last_monthly_grant_date`가 아닌 `monthly_granted_count` 구현과 통일.
- DB 설계에 `admin_audit_log`·`holiday_api_credentials`를 추가하고 현재 schema 20개 테이블과 대조.
- Claude/Codex 에이전트 수, Tailwind v4 CSS-first 규칙, 정책 설정 ACTIVE/PENDING 현황을 최신화.

## 3. 이전 Claude Code/Opus 세션에서 회수한 미구현·부분 구현

이전 Claude 세션 터미널은 현재 stale 상태라 새 질문을 직접 보낼 수 없었다. 대신 Orca에 보존된 Claude/Opus 감사 보고서와 `SESSION.md`, 현재 코드를 대조해 아래 내용을 기록한다.

| 영역 | 현재 판정 | 아직 남은 내용 |
|---|---|---|
| 미래 차감 연차 (`USER-1`/기존 B-1) | ❌ 미구현 | 기산일·미래 승인분·잔액/취소/리셋 규칙을 먼저 결정해야 한다. `validateDates`에 미래 상한과 기산일 검증이 없다. |
| 캘린더 하루 상세 | 🟡 부분 | 데스크톱은 일정 3개 이상일 때 `...더보기`로 상세 모달에 진입한다. 모바일은 선택 날짜 하단 요약에 전체 일정을 표시하고 셀 손잡이는 사용하지 않는다. |
| 공휴일 API 키·캘린더 이벤트 | 🟡 부분 | 2026년 API 응답(22건)을 확인했고 암호화 credential 저장 경계, 정상 응답 코드 검증, `HOLIDAY` 일정 표시를 구현했다. 실제 staging DB 시드·키 회전·관리자 수동 동기화 UI·브라우저 QA는 남아 있다. |
| 대시보드 다가오는 개인 일정 | 🟡 부분 | 가장 가까운 일정은 표시되지만 일정 데이터에 시간 필드가 없어 “몇 시”까지 표시할 수 없다. 시간 컬럼/API 추가 여부 결정 필요. |
| 이메일 계정 설정 (`USER-4`/기존 B-4) | ❌ 미구현 | 관리자가 발신 계정·키를 화면에서 바꾸는 기능이 없다. 키를 `PolicyConfig`에 평문 저장하면 `admin_audit_log`에 남을 수 있으므로 저장 위치·암호화·감사 마스킹을 먼저 결정한다. |
| 이메일 일괄 발송·수동 재발송 (`USER-5`/기존 B-5) | ❌ 미구현 | 건별 알림·자동 실패 재시도까지만 구현됐다. 30/60/90일·분기 잔여 연차 리마인더 설계 초안을 `docs/설계-초안/연차-소진-안내-메일-설계-2026-08-28.md`에 기록했으며, 대상 필터·발송량 제한·재발송 권한·이력 규칙 구현이 남아 있다. |
| 관리자 문의 (`USER-8`/기존 B-8) | ❌ 미구현 | 문의 생성·답변·알림·권한·보존 범위의 사양이 없다. |
| 미배정 부서 팀장 (`USER-7`/기존 B-7·B-9) | 🟡 부분/정책 미결 | 승격 시 부서 선택과 기본 부서 잠금은 반영됐지만, 미배정 부서 자체에 팀장을 허용할지는 결정되지 않았다. 미배정 사원끼리 일정이 보이는 스코프도 확인해야 한다. |
| 모바일 모달 (`MOBILE-1`/C-1) | 🟡 부분 | 등록 패널을 모바일 하단 시트로 열도록 반영했다. 끌어 올림·키보드 전환은 추가 QA/구현이 남아 있다. |
| 모바일 정책 설정 (`MOBILE-2`/C-2) | ❌ 미완료 | 미저장 경고·배지·확인창의 좁은 화면 대응이 필요하다. |
| 모바일 부서 관리 (`MOBILE-3`/C-3) | ❌ 미완료 | 계층 드래그 앤 드롭을 터치에서 어떻게 처리할지 결정하고 구현해야 한다. |
| 모바일 캘린더 (`MOBILE-4`/C-4) | 🟡 부분 | 정사각 날짜칸·색 점·좌우 월 스와이프·선택일 하단 일정/추가 버튼은 반영했다. 다중 날짜 선택 시트와 신청 화면 복귀 흐름은 남아 있다. |
| 키보드 월 이동 (`FEATURE-6`/D-6) | ❌ 미구현 | 마우스 화살표 가시성은 개선됐지만 키보드 화살표 이동은 남아 있다. |
| S-8 팀장 부서 이동 (`SEC-8`) | ❌ 미해결 | `changeDepartment`가 기존 `leader_id`를 정리하지 않는다. 팀장 해제·새 부서 이동·이동 거부 중 정책을 선택해야 하며, 기존 부서 사유 열람 권한도 재검증해야 한다. |
| S-9 팀장 동시성 (`SEC-9`) | ❌ 미해결 | 부서/사용자 잠금, 승인자 자격 재검증, 동시 갱신 시 409 UX를 설계·구현해야 한다. |

## 4. 운영·QA에서 아직 닫히지 않은 것

- 사용자 QA I-1~I-4: Google OAuth 계정이 없어 실제 온보딩·승인·반려 화면 캡처와 결과 회수가 막혀 있다.
- staging 백업 → preflight → 운영 대상 backfill 14개 → prod `validate` 기동 → smoke test 순서가 남아 있다.
- `email_history` 선행 테이블/컬럼과 `department.parent_id`를 포함한 backfill 전제조건을 실제 DB에서 확인해야 한다.
- OAuth `hd` claim 검증, Flyway 도입, DB 백업 cron, 퇴직자 보존/파기 절차는 별도 운영 작업이다.
- 사용자 QA 작업지의 계정 준비 절차·I-1 기대 화면·I-2 생일 입력 누락은 다음 QA 실행 전에 보완한다.

## 5. 다음 실행 순서

1. 공휴일 코드 보완은 끝났다(2026-09-05: 관리자 수동 동기화 UI, 외부 조회·저장 트랜잭션 분리, degrade는 통과 유지로 확정). 남은 것은 staging credential 시드 실행(절차는 `docs/12` 공휴일 절)과 공휴일 브라우저 QA(`docs/13` N-5~N-9)이며, 키 회전 UI는 2번의 발신 계정 설정과 함께 `/admin/integrations`에 반영했다.
2. ~~이메일 2단계~~ **완료(2026-09-05)** — W1~W4. 테이블 3개·ENUM 확장 2건과 `SchemaEnumConsistencyTest`, `SecretCipher` 일반화와 메일 계정 DB 저장, `SENDING` 선점, 온보딩 알림 4지점, 리마인더 ④잡, 관리자 API 13개, 관리자 화면 2개(`/admin/emails`·`/admin/integrations`). 정책 카탈로그는 ACTIVE 9개·PENDING_FEATURE 0개가 됐다. 남은 것은 아래 3~5번에 흡수했다.
3. ~~컨트롤러 테스트·`SENDING` 복구·공휴일 0건~~ **완료(2026-09-07)** — 양식·연동 컨트롤러 테스트 13건과 `SecurityAccessMatrixTest`의 실제 필터 체인 권한 검사, `sending_at` 기준 복구(backfill 14번), 공휴일 outcome 4종 구분(자동은 degrade·관리자 경로는 오류 표면화). **남은 전제**: 선점 소유자를 기록하지 않아 `SENDING` 복구는 여전히 단일 인스턴스 전제다 — 인스턴스를 늘리기 전에 다시 설계해야 한다.
4. staging 시드를 실행한다 — `APP_CREDENTIAL_ENCRYPTION_KEY`를 주입한 뒤 공휴일 키와 메일 계정을 각각 저장한다(키가 없으면 화면이 경고를 띄우고 저장이 잠긴다). 그 뒤 사용자 직접 QA I-1~I-4와 새 공휴일·이메일 QA 배치(`docs/13` N절·U절)를 실행하고 결과를 `docs/13`에 회수한다.
5. 이후 운영 DB 백업·preflight·backfill·`ddl-auto=validate` 기동·smoke test를 진행한다.

## 6. 기록 위치

- 프로젝트 진행도 메모리: `PROJECT_PROGRESS.md` (현재 파일)
- 현재 기능 상태: `docs/12-남은-작업.md`
- 시스템 구조: `docs/11-프로젝트-흐름.md`
- 수동 QA: `docs/13-QA-체크리스트.md`
- QA/preflight 증거: `docs/설계-초안/`
- 세션 포인터: `SESSION.md`
- Obsidian 세션 노트 및 `_Sessions.md`: `C:\Obsidian-lee\Lee's Brain\06 Claude Code Sessions`
